<%= form_with(model: sell, local: true) do |form| %>
  <% if sell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sell.errors.count, "error") %> prohibited this sell from being saved:</h2>

      <ul>
      <% sell.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :sell_client_id %>
  </div>

  <div class="field">
    <%= form.label :sell_date %>
    <%= form.date_select :sell_date, id: :sell_sell_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
