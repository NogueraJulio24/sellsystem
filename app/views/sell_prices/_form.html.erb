<%= form_with(model: sell_price, local: true) do |form| %>
  <% if sell_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sell_price.errors.count, "error") %> prohibited this sell_price from being saved:</h2>

      <ul>
      <% sell_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :sell_price_price %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :sell_price_total %>
  </div>

  <div class="field">
    <%= form.label :cant %>
    <%= form.number_field :cant, id: :sell_price_cant %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
